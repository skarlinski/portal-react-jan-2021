import React, { useState } from 'react';
import { InputGroup } from 'react-bootstrap';
import PortalSearchBar from '../components/searchbar/PortalSearchBar';

const ComponentZoo = () => {
   {
        const   title = "מס טלפון";
        const placeholder = "input-placeholder";
        return <div>
           myzoo
           <InputGroup title={title} placeholder={placeholder} handleChange={this.handleChange} />
           <InputGroup title={""} placeholder={placeholder} handleChange={this.handleChange} />
        </div>
    }
}
export default ComponentZoo;


